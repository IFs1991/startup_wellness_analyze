name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'

env:
  REGION: asia-northeast1
  SERVICE_NAME: startup-wellness-backend
  PROJECT_ID: startup-wellness

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        cache: 'pip'

    - name: Install dependencies
      run: |
        cd backend
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        cd backend
        python -m pytest

    - id: auth
      name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2 # v2 以降を使用
      with:
        workload_identity_provider: projects/startup-wellness/locations/global/workloadIdentityPools/github-actions-pool/providers/github-actions-provider # ← 正しい Workload Identity Provider のリソース名を設定
        service_account: startupwellness-github-actions-deploy@startup-wellness.iam.gserviceaccount.com # ← Workload Identity Federation 用に作成したサービスアカウントのメールアドレスを設定

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2
      with:
        project_id: ${{ env.PROJECT_ID }}

    - name: Authorize Docker push
      run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

    - name: Build and Deploy
      run: |
        cd backend
        gcloud builds submit --config cloudbuild.yaml \
          --substitutions=_REGION=${{ env.REGION }},_SERVICE_NAME=${{ env.SERVICE_NAME }}